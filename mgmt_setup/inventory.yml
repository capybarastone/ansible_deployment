---
all:
  children:
    mgmt:
      hosts:
        192.168.1.10
    backend:
      hosts:
        192.168.1.100
    endpoint_deb:
      hosts:
        192.168.1.102
    endpoint_win:
      hosts:
        192.168.1.101
      vars:
        ansible_shell_type: cmd
        ansible_become: yes
        ansible_connection: ssh
        ansible_become_user: deployment
        ansible_user: deployment
